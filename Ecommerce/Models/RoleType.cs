namespace Ecommerce.Models
{
    public enum RoleType
    {
        User,
        Admin,
        Dealer,
        SuparAdmin
    }
    public enum Status
    {
        pendding,
        Approve,
        Reject,
        Block
    }
    public enum DiscountType
    {
        Amount,
        Percentage
    }
}
